Crie um arquivo chamado pessoa.ts que exporta por padrão uma classe chamada Pessoa com os atributos que você considerar necessários e dois métodos chamados “falar” e "andar" cujas implementações imprimem os respectivos textos "<pessoa> falou" e "<pessoa> andou", troque <pessoa> pelo nome da pessoa. Você deve garantir que cada atributo seja acessado apenas por meio do método get.

Em outro arquivo, chamado app.ts que importe essa a classe Pessoa do arquivo pessoa.ts, instancie uma nova pessoa e chame os métodos falar e andar.

Altere a classe Pessoa e adicione um atributo chamado “peso” do tipo number. Você precisa criar o atributo privado e um método get para obter seu valor, da mesma forma que todos os outro atributos da classe. Faça as alterações necessárias para que todos os arquivos não apresentem erros.

Envie o código dos dois aquivos.
